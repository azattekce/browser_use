services:
  browser-test-app:
    build: .
    network_mode: "host"  # Host network kullan - Microsoft bypass için
    env_file:
      - .env
    volumes:
      - browser_test_instance:/app/instance:rw
      - browser_test_logs:/app/logs:rw
    environment:
      - FLASK_ENV=development
      - SECRET_KEY=development-secret-key-change-me
      - DATABASE_URL=sqlite:////app/instance/browser_test.db
      - HEADLESS=false
      - PYTHONPATH=/app
      - DISPLAY=:99
      - DOCKER_USER=docker_admin
      - PORT=5001
      # LLM API Keys - Host .env dosyasından alınacak
      - LLM_PROVIDER=${LLM_PROVIDER:-gemini}
      - LLM_MODEL=${LLM_MODEL:-gemini-flash-latest}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      # Browser automation config
      - WINDOW_WIDTH=1920
      - WINDOW_HEIGHT=1080
      - IMPLICIT_WAIT=5
      - EXPLICIT_WAIT=10
      - MAX_STEPS=50
    restart: unless-stopped
    privileged: true
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5001/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    # network_mode: host kullanıldığında networks tanımı kullanılamaz

volumes:
  browser_test_instance:
    driver: local
  browser_test_logs:
    driver: local